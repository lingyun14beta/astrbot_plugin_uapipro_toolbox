# 更新日志 (CHANGELOG)

> **如您在使用过程中遇到任何问题，请及时反馈，我们将持续优化。**

## [v3.5.0] - 2026-08-30
### ✨ 新功能
*   **🎨 新增图片抠图 `/u 抠图`**: 对接 UApiPro `/api/v1/image/matting` 接口，发送图片或引用图片即可自动识别主体、移除背景，返回透明主体（`cutout`）、灰度蒙版（`mask`）或纯色合成图（`background`），二次调用可配合面板设置证件照蓝底等效果；单次消耗 4 积分。
*   **⚙️ 新增「抠图设置」**: 配置面板新增分组，可独立设置分割模型（通用/轻量快速/人像特化/高锐利度边缘）、输出形态、合成底色（#RRGGBB）、输出格式（PNG/WebP/JPEG）、二值化阈值（0~1）与高斯羽化半径（0~64）；jpeg 与透明模式的冲突在本地预校验并给出友好提示，避免接口 400。
*   **🤖 新增 LLM 工具 `uapi_matting`**: 将抠图封装为 function calling 工具，LLM 可根据图片类型选择人像特化等模型并自动调用（工具返回处理信息，结果图片经 `/u 抠图` 指令发送）。
*   **🌍 i18n 同步**: `zh-CN.json`、`en-US.json`、`ru-RU.json` 三语文件补充「抠图设置」全部配置项翻译。

---

## [v3.4.0] - 2026-08-30
### ✨ 新功能
*   **⛏️ 新增 MC 基岩版服务器状态查询 `/u 基岩`**: 对接 UApiPro `/api/v1/game/minecraft/bedrockstatus` 接口，输入基岩版服务器地址（域名或 IP，默认端口 19132）即可实时查询在线状态、在线/最大人数、双行 MOTD、游戏模式（如 Survival）、版本与协议号、服务端 GUID 及云端测速延迟，卡片图片返回，单次消耗 1 积分。
*   **🤖 新增 LLM 工具 `uapi_mc_bedrock`**: 将基岩版服务器查询封装为 function calling 工具，LLM 可结合对话语境自动调用。

### 🔧 其他改进
*   **🔑 全面清理空 Token 鉴权头**: 排查并统一全部 21 个 API 模块与热榜调度器的本地 session 兜底分支——Token 为空时不再发送空的 `Token`/`Authorization: Bearer` 头，与 v3.3.0 共享 session 修复保持一致，消除访客额度在兜底路径下被 401 拒绝的隐患；`weather`/`ipquery` 同时改为仅在配置 Token 时携带 `token` 查询参数。

---

## [v3.3.0] - 2026-08-01
### ✨ 新功能
*   **👤 新增 GitHub 用户信息查询 `/u gh`**: 对接 UApiPro `/api/v1/github/user` 接口，支持用户名或主页链接，返回用户头像、个人简介、核心数据（关注者/公开仓库数）、所属组织、近一年贡献统计（总贡献/提交/Issue/PR 明细、最活跃月份）、Pinned 仓库及最近活跃仓库列表，单次消耗 2 积分。
*   **⚙️ 新增「GitHub 用户查询设置」**: 配置面板新增分组，可独立控制「获取贡献活动数据」「贡献活动范围（all/organization）」「组织登录名」「附带 Pinned 仓库」「附带最近活跃仓库」「仓库列表数量（默认 5，1-100）」。填写组织名后自动切换为组织级贡献统计；关闭对应开关时不传相关参数，遵循接口参数约束。
*   **🤖 新增 LLM 工具 `uapi_github_user`**: 将 GitHub 用户画像查询封装为 function calling 工具，LLM 可结合对话语境自动调用。

### 🔧 其他改进
*   **🔑 修复空 Token 访客模式失效**: 原先未配置 Token 时，共享 session 仍携带空 `Authorization: Bearer` 头，被 UApiPro 判定为无效密钥并拒绝降级到访客额度，导致全部指令在访客模式下失效。现改为 Token 为空时不发送鉴权头，访客额度恢复可用（`/u gh` 与全部既有指令均受益）。

### 🌐 国际化
*   **🌍 i18n 同步**: `zh-CN.json`、`en-US.json`、`ru-RU.json` 三语文件补充「GitHub 用户查询设置」全部配置项翻译。

---

## [v3.2.1] - 2026-07-22
### 🌐 国际化
*   **🌍 新增 i18n 支持**: 在 `.astrbot-plugin/i18n/` 下新增 `zh-CN.json`、`en-US.json`、`ru-RU.json` 三语翻译文件，覆盖插件元数据（名称、描述）及全部配置项（描述、提示、下拉标签），WebUI 可根据当前语言自动切换显示文案。

---

## [v3.2.0] - 2026-06-27
### ✨ 新功能
*   **🎯 新增高级一言**: `apis/hitokoto.py` 新增 `fetch_advanced()`，对接 UApiPro `/api/v1/saying/random` 接口，支持按运行模式（随机/每日/场景推荐/此刻）、推荐场景、语料来源、分类、标签筛选语录；原基础随机一言接口（`fetch()`）保留不变。
*   **⚙️ 新增「高级一言设置」**: 配置面板新增对应分组，含总开关「启用高级一言」（默认关闭，关闭时行为与旧版本完全一致）及运行模式/场景/来源/分类/标签等子配置，下拉选项均带中文显示名。
*   **🤖 LLM 工具动态切换**: `uapi_hitokoto` 工具按「启用高级一言」开关动态二选一注册——关闭时为零参数基础版，开启后切换为开放 mode/scene/source/category/tag 全部参数的高级版，供 LLM 结合对话语境自主选择（如用户说晚安自动联想 `scene=night`）。面板保存配置触发热重载后自动同步，无需手动操作。

### 🔧 其他改进
*   **🧹 代码清理**: 修复 `bili.py`、`icp.py`、`news.py`、`qrcode.py`、`hotboard/renderer.py` 中遗留的 lint 问题（多余 f-string 前缀、未使用的 import、单行多语句），均为代码风格调整，不影响任何功能行为。

---

## [v3.1.0] - 2026-06-27
### ✨ 新功能
*   **🔍 新增图片文字识别 `/u ocr`**: 接入 UApiPro 通用 OCR 接口，支持直接发图或引用一张图片后发送指令触发识别，返回逐字提取的纯文本结果；QQ（aiocqhttp）平台自动使用合并转发消息发送，避免长文本刷屏，其他平台降级为普通文本消息。
*   **⚙️ 新增 OCR 识图设置**: 配置面板新增「OCR 识图设置」分组，可独立开启「返回 Markdown 格式」（适合票据、表格类图片）与「启用方向校正」（自动校正手机拍摄导致的旋转/倾斜），开启 Markdown 后识别结果优先返回整理后的 Markdown 文本。
*   **🤖 新增 LLM 工具 `uapi_ocr`**: 读取当前对话中的图片（含引用回复）进行精确文字识别，**返回精确文字而非图片描述**。

### 📝 其他
*   **⚠️ 补充快递查询积分消耗提醒: `/u 快递` 单次调用消耗 40 积分，远高于其他指令，README 指令表格与「关于 API 额度」章节均已补充说明，避免误解配额消耗情况。**

  

---

<details>
<summary>📋 点击查看历史更新日志</summary>

## [v3.0.0] - 2026-05-31
### ✨ 新功能
*   **📊 新增全网热榜聚合 `/u 热榜 <平台>`**: 支持哔哩哔哩、A站、HelloGitHub、网易云音乐、QQ音乐、微信读书共 6 个平台实时热榜，每 5 分钟自动更新。不带参数发送 `/u 热榜` 显示平台列表，输入错误平台名时给出提示。
*   **🖼️ 平台专属渲染卡片**: 新增独立热榜渲染引擎 `hotboard/renderer.py`，哔哩哔哩（Top 10 含封面）、网易云音乐、QQ音乐、微信读书（Top 10 含专辑/书籍封面）支持图片展示；HelloGitHub（Top 10）展示编程语言标签与项目简介；A站（Top 10）纯文字列表。各平台图标从本地 `hotboard/icons/` 读取 base64 嵌入，完全离线。
*   **📁 模块化热榜架构**: 新增 `hotboard/` 目录，`fetcher.py` 负责请求与错误处理，各平台解析逻辑独立维护于单独文件，后续扩展新平台只需新增一个解析文件与图标。
*   **⬇️ 文本降级支持**: 渲染服务故障时自动切换文本模式，各平台副信息（UP主、歌手、编程语言、作者等）及原始链接完整保留，不依赖 HTML 解析。
*   **🤖 新增 LLM 工具 `uapi_hotboard`**: 将热榜封装为 function calling 工具，LLM 可主动查询指定平台热榜并基于内容回答问题（如"今天B站热榜有没有关于AI的视频"），返回含标题、热度、副信息及原始链接的结构化文本，共 **16 个工具**。

### 🔧 其他改进
*   **📐 渲染截图留白修复**: `card_renderer.py` 改用 `display: table` + viewport 宽度锁定，渲染截图不再出现两侧及底部多余留白。
*   **🔁 渲染策略对齐**: `cmd_hotboard` 补全第二条渲染策略（`quality: 100 + ultra`），与 `_send_analysis_report` 保持四级策略一致。

## [v2.3.1] - 2026-05-27
### 🐛 Bug 修复
*   **📰 修复定时早报重复推送**: 修复重载/重启插件后定时新闻重复推送的问题，改用 AstrBot 内置 KV 存储持久化推送日期。

## [v2.3.0] - 2026-05-27
### 🐛 Bug 修复
*   **📰 修复定时早报无法推送**: 修正 `context.get_all_platforms()` 不存在导致平台 ID 退回 `"default"`，进而触发 `cannot find platform` 报错的问题，改为直接读取 `platform_manager.platform_insts`。

### 📝 其他改进
*   **🏷️ 补全面板指令描述**: 所有 `/u` 子指令补充 `desc=` 参数，面板不再显示"无描述"。
*   **📖 修正 README**: 目录名、`/u b站` → `/u bili`、补充 `/u 帮助` 条目、`apis/` 列表末尾加 `...`。
*   **🛠️ 修正 `bili.py` 用法提示**: `usage_hint` 中的指令名与实际注册名保持一致。

## [v2.2.1] - 2026-05-03
### ⚙️ 配置项新增
*   **🔀 新增 LLM 工具调用开关**: 在插件配置中新增「允许 LLM 自动调用工具」选项，默认关闭。开启后 LLM 可根据对话内容自动调用查询工具；关闭后仅支持 `/u` 指令手动触发，两种模式可随时切换，重载插件生效。

## [v2.2.0] - 2026-05-03
### 🤖 LLM 工具调用支持
*   **🛠️ 新增 `llm_tools.py`**: 将 15 项查询指令封装为 AstrBot function calling 工具，LLM 可根据对话内容自动决策调用，用户无需手动输入指令前缀。
*   **📦 工具覆盖范围**: 天气、IP 查询、MC 服务器、MC 玩家、WHOIS、ICP 备案、快递追踪、万年历、GitHub、Steam、B站、答案之书、一言、Epic 免费游戏、随机字符串，共 **15 个工具**。
*   **🚫 排除不适合工具化的指令**: 随机图片、必应壁纸、每日新闻（返回图片，工具模式无意义）及二维码生成不纳入工具列表。


## [v2.1.2] - 2026-04-19
### 🔧 兼容性修复
*   **🏷️ 兼容 AstrBot 指令别名**: 新增 `_extract_arg` 辅助方法，修复通过 AstrBot WebUI「重命名指令」功能设置别名后，参数解析失败的问题。根本原因是 AstrBot 在别名触发时不改写 `event.message_str`，导致原有正则匹配不到命令前缀，参数被整段错误透传。现改为正则优先、剥离别名 token 兜底的双重策略，全部 13 处参数提取统一走此方法。

## [v2.1.1] - 2026-04-18
### 🛡️ 安全修复 & 问题修正

*   **🔒 补全鉴权头部** (`bing_daily.py`): 本地 session 创建时补充缺失的 `Authorization: Bearer` 头部，与其他所有模块保持一致。
*   **❌ 修复日期参数静默忽略** (`bing_daily.py`): 用户输入格式不合法的日期（如 `/u 必应 2023`）时，原版本会静默忽略并返回今日壁纸，现改为返回明确的格式错误提示，要求使用 `YYYY-MM-DD` 格式。
*   **📏 新增输入长度校验** (`mc_user.py`): 限制 MC 用户名最长 20 字符，防止超长字符串透传至上游 API。

## [v2.1.0] - 2026-04-12
### 🔧 渲染引擎修复
*   **🖼️ 修复渲染调用方式**: 修正 `html_render` 参数传递错误，改为正确的位置参数调用方式，与 AstrBot 渲染引擎兼容。
*   **🎯 多策略渲染**: 引入四级渲染策略（Ultra PNG → Ultra JPEG → High JPEG → 普通 JPEG），自动降级重试，提升出图成功率。
*   **✅ 图片合法性校验**: 渲染结果写入前校验 magic bytes（JPEG/PNG），防止服务器返回错误页被当作图片发送。
*   **📝 文本降级保护**: 渲染失败时自动 fallback 文本模式，修复因无 `yield` 导致消息被 LLM 接管的问题。
*   **📐 居中布局修复**: 修正卡片偏左问题，`body` 改用 `min-width: 100vw` 铺满视口后居中。

## [v2.0.0] - 2026-04-12
### ✨ 架构升级
*   **🧩 功能大合集**: 加入集成 GitHub 深度分析、B站综合档案（用户信息+投稿）、Steam 资料卡、快递实时追踪等 **10+** 项指令。
*   **🎨 全新 UI 引擎**: 重构 `card_renderer.py`，引入 **纯白设计**。
*   **🛡️ 深度安全加固**: 全量模块实现 **SSRF 域名白名单**校验，强制 HTTPS，所有图片抓取均在后台完成并转为 Base64 嵌入。
*   **🛠️ 健壮性增强**: 重写 `main.py` 的文本解析器，支持对新版 `item-label` 的精准提取，确保渲染故障时文本降级依然完美。

## [v1.0.0] - 初始版本发布
*   **发布**: UApiPro 工具箱正式上线。
*   **首发功能**:
    *   天气预报。
    *   IP 归属地查询。
    *   Minecraft 服务器状态 Ping。
    *   每日新闻早报。
    *   随机一言

</details>
